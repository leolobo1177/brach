(function () {
  const slides = [
    {
      number: "01",
      label: "Objetivo da marca",
      nav: "Objetivo",
      title: "Uma marca criada para acolher, ensinar e encantar",
      image: "./assets/01.png",
      alt: "Logo da Ensinarte com Celina e Axol sobre nuvem lilas",
      accent: "#f0b8ff",
      body: [
        "A Ensinarte nasceu com o objetivo de transformar o processo de aprendizagem em uma experiencia mais leve, afetiva e criativa.",
        "A marca foi desenvolvida para uma professora que trabalha com criancas especiais e precisava de uma identidade que transmitisse acolhimento, confianca e imaginacao.",
        "Por isso, a identidade combina cores suaves, formas arredondadas, personagens carismaticos e uma linguagem visual ludica, criando uma marca proxima, segura e cheia de possibilidades."
      ]
    },
    {
      number: "02",
      label: "Celina, o alter ego da marca",
      nav: "Celina",
      title: "Celina: a crianca interior que ensina com afeto",
      image: "./assets/02.png",
      alt: "Celina em destaque na identidade da Ensinarte",
      accent: "#c79cff",
      body: [
        "Celina foi criada como o alter ego da professora. A personagem representa uma versao inspirada nela quando crianca, trazendo para a marca uma presenca mais humana, proxima e afetiva.",
        "A ideia era construir uma personagem amigavel, curiosa e cheia de energia criativa, reconhecivel pelas criancas de forma imediata.",
        "Ela funciona como uma ponte entre a educadora, os alunos e o universo da aprendizagem, tornando conteudos e comunicacoes mais acessiveis, leves e memoraveis."
      ]
    },
    {
      number: "03",
      label: "Axol, o mascote de apoio",
      nav: "Axol",
      title: "Axol: o companheiro que acolhe e acompanha",
      image: "./assets/03.png",
      alt: "Axol em destaque como mascote da Ensinarte",
      accent: "#b897ff",
      body: [
        "Axol foi desenvolvido como o mascote de apoio da Ensinarte. Inspirado em uma criatura doce, curiosa e imaginativa, ele representa cuidado, companhia e incentivo durante o processo de aprendizagem.",
        "Enquanto Celina traduz a energia da professora em forma de personagem, Axol entra como um amigo simbolico para as criancas.",
        "Sua aparencia macia, suas cores suaves e sua expressao amigavel foram pensadas para gerar identificacao e conforto, principalmente em uma marca voltada para criancas especiais."
      ]
    },
    {
      number: "04",
      label: "Logo completo com personagens",
      nav: "Assinatura",
      title: "Uma assinatura visual com personalidade",
      image: "./assets/04.png",
      alt: "Logo completo da Ensinarte com Celina e Axol",
      accent: "#f4c2ff",
      body: [
        "A versao completa do logo une o nome Ensinarte aos personagens Celina e Axol, criando uma assinatura visual forte, afetiva e facilmente reconhecivel.",
        "Essa composicao foi pensada para os principais pontos de contato da marca, como capa de materiais, apresentacoes, redes sociais e pecas institucionais.",
        "A presenca dos personagens aproxima a marca do universo infantil, enquanto o lettering manual e a composicao em nuvem reforcam criatividade, imaginacao e aprendizagem leve."
      ]
    },
    {
      number: "05",
      label: "Logo principal",
      nav: "Logo",
      title: "Leveza, criatividade e memoria visual",
      image: "./assets/05.png",
      alt: "Versao principal do logo da Ensinarte",
      accent: "#f7d0ff",
      body: [
        "A versao principal do logo trabalha com uma composicao mais limpa, mantendo os elementos centrais da identidade: o nome Ensinarte, a nuvem, as estrelas e a faixa de apoio com o conceito Aprendizagem Criativa.",
        "Ela funciona bem em materiais pedagogicos, papelaria, redes sociais e aplicacoes onde a marca precisa aparecer de forma mais simples e direta.",
        "O resultado e uma assinatura delicada, ludica e memoravel, com equilibrio entre fantasia e clareza."
      ]
    },
    {
      number: "06",
      label: "Variacoes do logo",
      nav: "Variacoes",
      title: "Uma marca preparada para diferentes usos",
      image: "./assets/06.png",
      alt: "Variacoes do logo da Ensinarte",
      accent: "#e8c6ff",
      body: [
        "Para garantir flexibilidade, foram criadas diferentes variacoes do logo, permitindo que a marca seja aplicada em fundos claros, escuros, coloridos e materiais diversos.",
        "Essas versoes ajudam a preservar a leitura, o contraste e a consistencia visual em qualquer situacao.",
        "Essa etapa e fundamental para que a identidade funcione no dia a dia, tanto em ambientes digitais quanto em materiais impressos."
      ]
    },
    {
      number: "07",
      label: "Aplicacao no Instagram",
      nav: "Instagram",
      title: "A marca aplicada no digital",
      image: "./assets/07.png",
      alt: "Perfil do Instagram da Ensinarte",
      accent: "#bfa0ff",
      body: [
        "A identidade da Ensinarte tambem foi pensada para redes sociais, criando um perfil visualmente organizado, acolhedor e reconhecivel.",
        "A aplicacao no Instagram mostra como os personagens, cores e elementos graficos podem construir uma presenca digital consistente.",
        "Cada detalhe foi pensado para aproximar a marca das familias e tornar a comunicacao mais amigavel, educativa e encantadora."
      ]
    },
    {
      number: "08",
      label: "Ecobag e expansao da marca",
      nav: "Ecobag",
      title: "Elementos que expandem o universo da marca",
      image: "./assets/08.png",
      alt: "Aplicacao da identidade da Ensinarte em ecobag",
      accent: "#f5c3ff",
      body: [
        "Alem do logo, a marca ganhou elementos visuais que ajudam a criar um universo proprio. A aplicacao na ecobag mostra como Axol pode virar padrao grafico, funcionando como um detalhe divertido e reconhecivel.",
        "Esse tipo de desdobramento fortalece a identidade e permite que a marca esteja presente em materiais fisicos, brindes, eventos e kits pedagogicos.",
        "A linguagem visual deixa de viver apenas no logo e passa a criar uma experiencia completa em todos os pontos de contato."
      ]
    },
    {
      number: "09",
      label: "Sistema visual completo",
      nav: "Sistema",
      title: "Consistencia em todos os pontos de contato",
      image: "./assets/09.png",
      alt: "Aplicacoes graficas da identidade visual da Ensinarte",
      accent: "#e2b4ff",
      body: [
        "A identidade visual da Ensinarte foi construida para funcionar como um sistema. Logo, personagens, cores, formas e elementos graficos se combinam para criar uma comunicacao consistente em diferentes materiais.",
        "Nas aplicacoes, a marca mantem sua essencia ludica e acolhedora, reforcando a ideia de uma aprendizagem criativa, afetiva e proxima das criancas.",
        "Esse fechamento mostra que a identidade e memoravel nao apenas por um elemento isolado, mas pelo conjunto de decisoes visuais trabalhando em sintonia."
      ]
    }
  ];

  const mediaContainer = document.getElementById("storyMedia");
  const counter = document.getElementById("storyCounter");
  const label = document.getElementById("storyLabel");
  const title = document.getElementById("storyTitle");
  const body = document.getElementById("storyBody");
  const toc = document.getElementById("storyToc");
  const mobileList = document.getElementById("mobileStoryList");
  const storySection = document.querySelector(".project-story");
  const storySticky = document.querySelector(".project-story__sticky");
  const revealLeft = Array.from(document.querySelectorAll("[data-reveal-left]"));
  const revealRight = Array.from(document.querySelectorAll("[data-reveal-right]"));

  let activeIndex = 0;
  let imageNodes = [];
  let tocButtons = [];
  let storyTrigger = null;

  function setBody(slide) {
    body.innerHTML = slide.body.map(function (paragraph) {
      return "<p>" + paragraph + "</p>";
    }).join("");
  }

  function createStoryImages() {
    imageNodes = slides.map(function (slide, index) {
      const img = document.createElement("img");
      img.className = "project-story__image" + (index === 0 ? " is-active" : "");
      img.src = slide.image;
      img.alt = slide.alt;
      img.width = 681;
      img.height = 482;
      mediaContainer.appendChild(img);
      return img;
    });
  }

  function createToc() {
    tocButtons = slides.map(function (slide, index) {
      const button = document.createElement("button");
      button.type = "button";
      button.innerHTML = '<span class="project-story__nav-title">' + slide.nav + "</span>";
      if (index === 0) {
        button.classList.add("is-active");
      }
      button.addEventListener("click", function () {
        if (!storySection) return;
        const segment = window.innerHeight * 0.82;
        const targetTop = storySection.offsetTop + segment * index;
        window.scrollTo({ top: targetTop, behavior: "smooth" });
      });
      toc.appendChild(button);
      return button;
    });
  }

  function createMobileCards() {
    if (!mobileList) return;

    mobileList.innerHTML = slides.map(function (slide) {
      const paragraphs = slide.body.map(function (paragraph) {
        return "<p>" + paragraph + "</p>";
      }).join("");

      return (
        '<article class="project-mobile-card" data-mobile-card>' +
          '<div class="project-mobile-card__frame">' +
            '<img src="' + slide.image + '" alt="' + slide.alt + '" width="681" height="482" />' +
          "</div>" +
          '<div class="project-mobile-card__meta">' +
            '<p class="project-mobile-card__label">' + slide.label + "</p>" +
            '<p class="project-mobile-card__count">' + slide.number + "</p>" +
          "</div>" +
          '<h3 class="project-mobile-card__title">' + slide.title + "</h3>" +
          '<div class="project-mobile-card__body">' + paragraphs + "</div>" +
        "</article>"
      );
    }).join("");
  }

  function markToc(index) {
    tocButtons.forEach(function (button, buttonIndex) {
      button.classList.toggle("is-active", buttonIndex === index);
    });
  }

  function updateAccent(slide) {
    document.documentElement.style.setProperty("--story-accent", slide.accent);
  }

  function switchSlide(index, immediate) {
    const nextIndex = Math.max(0, Math.min(slides.length - 1, index));
    const nextSlide = slides[nextIndex];
    const currentImage = imageNodes[activeIndex];
    const nextImage = imageNodes[nextIndex];
    const direction = nextIndex > activeIndex ? 1 : -1;

    if (!nextSlide) return;

    markToc(nextIndex);
    updateAccent(nextSlide);

    if (
      immediate ||
      !window.gsap ||
      !currentImage ||
      !nextImage ||
      nextIndex === activeIndex
    ) {
      counter.textContent = nextSlide.number;
      label.textContent = nextSlide.label;
      title.textContent = nextSlide.title;
      setBody(nextSlide);

      imageNodes.forEach(function (node, imageIndex) {
        node.classList.toggle("is-active", imageIndex === nextIndex);
        node.style.opacity = imageIndex === nextIndex ? "1" : "0";
        node.style.transform = "translateX(0px) scale(1)";
      });

      activeIndex = nextIndex;
      return;
    }

    const outgoingText = [counter, label, title].concat(Array.from(body.children));

    nextImage.classList.add("is-active");

    const tl = window.gsap.timeline({
      defaults: { ease: "power3.out" }
    });

    tl.to(outgoingText, {
      x: -direction * 34,
      autoAlpha: 0,
      duration: 0.22,
      stagger: 0.03
    }, 0);

    tl.to(currentImage, {
      x: -direction * 120,
      autoAlpha: 0,
      scale: 0.97,
      duration: 0.38,
      ease: "power2.inOut"
    }, 0);

    tl.set(nextImage, {
      x: direction * 140,
      autoAlpha: 0,
      scale: 1.02
    }, 0.04);

    tl.to(nextImage, {
      x: 0,
      autoAlpha: 1,
      scale: 1,
      duration: 0.68,
      ease: "power3.out"
    }, 0.08);

    tl.add(function () {
      counter.textContent = nextSlide.number;
      label.textContent = nextSlide.label;
      title.textContent = nextSlide.title;
      setBody(nextSlide);
    }, 0.18);

    tl.fromTo(
      [counter, label, title].concat(Array.from(body.children)),
      {
        x: direction * 38,
        autoAlpha: 0
      },
      {
        x: 0,
        autoAlpha: 1,
        duration: 0.46,
        stagger: 0.04
      },
      0.22
    );

    tl.set(currentImage, {
      opacity: 0,
      x: 0,
      scale: 1
    }, 0.78);

    activeIndex = nextIndex;
  }

  function setStoryHeight() {
    if (!storySection || window.innerWidth <= 960) {
      if (storySection) {
        storySection.style.height = "";
      }
      return;
    }

    const segment = window.innerHeight * 0.82;
    storySection.style.height = window.innerHeight + segment * (slides.length - 1) + "px";
  }

  function setupStoryTrigger() {
    if (!window.gsap || !window.ScrollTrigger || !storySection || !storySticky) return;

    if (storyTrigger) {
      storyTrigger.kill();
      storyTrigger = null;
    }

    if (window.innerWidth <= 960) {
      setStoryHeight();
      switchSlide(activeIndex, true);
      return;
    }

    setStoryHeight();

    storyTrigger = window.ScrollTrigger.create({
      trigger: storySection,
      start: "top top",
      end: function () {
        return "+=" + window.innerHeight * (slides.length - 1) * 0.82;
      },
      pin: storySticky,
      scrub: 0.7,
      invalidateOnRefresh: true,
      onUpdate: function (self) {
        const nextIndex = Math.min(
          slides.length - 1,
          Math.round(self.progress * (slides.length - 1))
        );

        if (nextIndex !== activeIndex) {
          switchSlide(nextIndex, false);
        }
      }
    });
  }

  function setupHeroReveal() {
    if (!window.gsap) return;

    if (revealLeft.length) {
      window.gsap.from(revealLeft, {
        x: -38,
        autoAlpha: 0,
        duration: 0.86,
        stagger: 0.08,
        ease: "power3.out",
        delay: 0.06
      });
    }

    if (revealRight.length) {
      window.gsap.from(revealRight, {
        x: 46,
        autoAlpha: 0,
        duration: 0.9,
        stagger: 0.08,
        ease: "power3.out",
        delay: 0.12
      });
    }
  }

  function setupMobileReveals() {
    if (!window.gsap || !window.ScrollTrigger) return;

    Array.from(document.querySelectorAll("[data-mobile-card]")).forEach(function (card, index) {
      window.gsap.from(card, {
        x: index % 2 === 0 ? -22 : 22,
        y: 20,
        autoAlpha: 0,
        duration: 0.74,
        ease: "power3.out",
        scrollTrigger: {
          trigger: card,
          start: "top 86%"
        }
      });
    });
  }

  function handleResize() {
    setStoryHeight();
    if (window.ScrollTrigger) {
      window.ScrollTrigger.refresh();
    }
  }

  createStoryImages();
  createToc();
  createMobileCards();
  switchSlide(0, true);

  window.addEventListener("load", function () {
    setupHeroReveal();
    setupStoryTrigger();
    setupMobileReveals();
    handleResize();
  });

  window.addEventListener("resize", handleResize);
})();
